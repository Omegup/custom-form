/** Italic legend for review status border colors. */
export const ReviewStatusNote = () => (
  <p
    style={{
      margin: "8px 0 0",
      fontSize: 12,
      color: "#888",
      fontStyle: "italic",
    }}
  >
    Follow / review — status border colors: green = commented, amber = needs
    attention, grey = idle.
  </p>
);
